---
/**
 * MobileHeader — the floating lavender nav pill from the 390px design (below lg).
 *
 * Rendered once at the page root (see Layout.astro), OUTSIDE `.page-content`, so
 * it's never trapped inside a section's stacking context: pages wrap their
 * content sections in `isolate` wrappers, and a fixed nav nested in the hero
 * would be out-painted by those sibling wrappers no matter its z-index. Kept at
 * the body level it always stays on top. The desktop header still lives in
 * Navbar (it's `absolute`, anchored per-hero, and tone-aware).
 */
import site from "../../data/site.json";
import Button from "../ui/Button.astro";

// Mobile menu content is authored separately from the desktop nav so the two
// can diverge (the mobile panel has an expandable Solutions section).
const mobileItems = site.mobileMenu.items;
---

{/* `fixed` so it stays put while the page scrolls. */}
<header
  class="fixed inset-x-0 top-0 z-30 px-5 pt-[14px] lg:hidden"
  data-mobile-header
>
  <div
    class="flex items-center justify-between rounded-full bg-lavender py-1 pl-8 pr-1 backdrop-blur-[20px]"
  >
    <!-- Sword icon-mark (left) -->
    <a
      href="/"
      class="text-black"
      aria-label={`${site.logo.bold} ${site.logo.regular}`}
    >
      <svg
        class="block h-6 w-[13.42px]"
        viewBox="0 0 13.4208 24"
        fill="none"
        aria-hidden="true"
      >
        <path
          d="M6.12011 0.0209433C0.711791 0.43287 -1.8992 7.11858 1.57389 11.3744C5.01554 15.5908 11.7327 14.1051 13.1606 8.77785C14.4358 4.02219 10.8549 -0.340071 6.12011 0.0209433ZM10.3745 7.75267C9.67177 11.3073 4.58899 11.6706 3.36544 8.3775C2.50334 6.05636 3.68423 3.33487 6.21664 3.06874C8.9803 2.77715 10.9335 4.91778 10.3722 7.75267H10.3745Z"
          fill="currentColor"></path>
        <path
          d="M2.3471 19.7724C2.53793 23.0099 6.00877 25.0117 8.79712 23.4704C11.3587 22.0518 11.8526 18.4348 9.79616 16.315C6.96067 13.3921 2.10014 15.6276 2.34485 19.7724H2.3471Z"
          fill="currentColor"></path>
      </svg>
    </a>

    <!-- MENU toggle (center) -->
    <button
      type="button"
      class="flex items-center gap-2 px-6 text-sm font-semibold uppercase text-black"
      aria-expanded="false"
      aria-controls="mobile-menu-panel"
      data-menu-toggle
    >
      Menu
      <svg class="size-4" viewBox="0 0 16 16" fill="none" aria-hidden="true">
        <circle cx="3" cy="3" r="3" fill="currentColor"></circle>
        <circle cx="13" cy="3" r="3" fill="currentColor"></circle>
        <circle cx="3" cy="13" r="3" fill="currentColor"></circle>
        <circle cx="13" cy="13" r="3" fill="currentColor"></circle>
      </svg>
    </button>

    <!-- Primary CTA (right) -->
    <Button variant="solid" href={site.cta.href}>{site.cta.label}</Button>
  </div>

  <!-- Slide-down nav panel revealed by MENU. -->
  <div
    id="mobile-menu-panel"
    class="mt-2 hidden rounded-[40px] bg-lavender p-8 backdrop-blur-[20px]"
    data-menu-panel
  >
    <nav
      class="flex flex-col font-mono text-sm uppercase tracking-[-0.14px] text-black"
    >
      {
        mobileItems.map((item, i) => {
          // Items are separated by a divider; the outer edges sit flush against
          // the panel padding (first: bottom-only, last: top-only, no border).
          const edge =
            i === mobileItems.length - 1
              ? "pt-3"
              : i === 0
                ? "border-b border-black/24 pb-3"
                : "border-b border-black/24 py-3";
          return item.groups ? (
            <div class={edge} data-submenu>
              <button
                type="button"
                class="flex w-full items-center justify-between font-mono uppercase tracking-[-0.14px]"
                aria-expanded="false"
                data-submenu-toggle
              >
                <span>{item.label}</span>
                <svg
                  class="size-3 transition-transform duration-200"
                  viewBox="0 0 12 12"
                  fill="none"
                  aria-hidden="true"
                  data-submenu-caret
                >
                  <path
                    d="M2.5 4.5 6 8l3.5-3.5"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              </button>

              <div class="hidden flex-col gap-6 pt-6" data-submenu-panel>
                {item.groups.map((group) => (
                  <div class="flex flex-col gap-4">
                    <p class="w-full">{group.label}</p>
                    <div class="flex flex-col gap-6">
                      {group.items.map((sub) => (
                        <a href={sub.href} class="flex items-start gap-4">
                          <span class="flex size-10 shrink-0 items-center justify-center rounded-full border-[1.5px] border-dotted border-black">
                            {sub.icon && (
                              <img
                                src={sub.icon}
                                alt=""
                                aria-hidden="true"
                                class="size-6"
                              />
                            )}
                          </span>
                          <span class="flex min-w-0 flex-1 flex-col">
                            <span class="text-black">{sub.title}</span>
                            <span class="text-ink-mid">{sub.subtitle}</span>
                          </span>
                        </a>
                      ))}
                    </div>
                  </div>
                ))}
              </div>
            </div>
          ) : (
            <a href={item.href} class={edge}>
              {item.label}
            </a>
          );
        })
      }
    </nav>
  </div>
</header>

<script>
  const header = document.querySelector<HTMLElement>("[data-mobile-header]");
  if (header) {
    const toggle =
      header.querySelector<HTMLButtonElement>("[data-menu-toggle]");
    const panel = header.querySelector<HTMLElement>("[data-menu-panel]");

    const setOpen = (open: boolean) => {
      toggle?.setAttribute("aria-expanded", String(open));
      panel?.classList.toggle("hidden", !open);
    };

    toggle?.addEventListener("click", (e) => {
      e.stopPropagation();
      setOpen(toggle.getAttribute("aria-expanded") !== "true");
    });

    // Expandable sub-sections inside the panel (e.g. Solutions).
    panel
      ?.querySelectorAll<HTMLElement>("[data-submenu]")
      .forEach((submenu) => {
        const subToggle = submenu.querySelector<HTMLButtonElement>(
          "[data-submenu-toggle]",
        );
        const subPanel = submenu.querySelector<HTMLElement>(
          "[data-submenu-panel]",
        );
        const caret = submenu.querySelector<HTMLElement>(
          "[data-submenu-caret]",
        );
        subToggle?.addEventListener("click", () => {
          const open = subToggle.getAttribute("aria-expanded") !== "true";
          subToggle.setAttribute("aria-expanded", String(open));
          subPanel?.classList.toggle("hidden", !open);
          subPanel?.classList.toggle("flex", open);
          caret?.classList.toggle("rotate-180", open);
        });
      });

    document.addEventListener("click", (e) => {
      if (!header.contains(e.target as Node)) setOpen(false);
    });
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") setOpen(false);
    });
  }
</script>
