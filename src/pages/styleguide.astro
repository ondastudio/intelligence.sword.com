---
/**
 * Living style guide — a visual catalog of design tokens and every component
 * in the library. As components are extracted from the design, render each one
 * here (with its variants) so anyone can see what already exists before
 * building something new.
 */
import Layout from '../layouts/Layout.astro';
---

<Layout title="Style Guide · intelligence.sword.com">
  <main>
    <!--
      TODO: as the library grows, add sections here:
        1. Colors      — swatches for each token in @theme
        2. Typography  — headings, body, captions
        3. Components  — every component with all its variants
    -->
  </main>
</Layout>
