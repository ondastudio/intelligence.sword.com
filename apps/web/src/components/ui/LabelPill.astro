---
/**
 * LabelPill — outlined uppercase pill used as a category label.
 * From the design's bordered "Label" element on every feature card
 * (e.g. "ALWAYS ON", "HUMANS IN THE LOOP"). Distinct from Eyebrow, which is a
 * borderless dot-prefixed label.
 */
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<span
  class={`inline-flex h-fit items-center justify-center rounded-full border border-black/24 px-4 py-[7px] text-center text-sm font-semibold uppercase leading-tight text-black ${className}`}
>
  <slot />
</span>
