# Component registry

The single source of truth for what already exists in the library. **Check this
list before creating any component.** When you add or change a component, update
the table and showcase it on the `/styleguide` page.

All components are extracted from the Figma design (Sword Intelligence 2026) and
styled with design tokens from `src/styles/global.css` (`bg-brand`, `font-serif`, …).

## UI primitives — `src/components/ui/`

| Component | Path | Props | Description |
| --------- | ---- | ----- | ----------- |
| Button | `ui/Button.astro` | `variant` (`solid`\|`glass`), `href`, `caret`, `type`, `class` | Pill action. `solid` = purple CTA, `glass` = translucent nav item. Renders `<a>` if `href`, else `<button>`. `caret` adds a dropdown chevron. |
| Eyebrow | `ui/Eyebrow.astro` | `class`, `dotClass`, `textClass` + slot | Small uppercase label preceded by a dot (the design's "Label"). `dotClass` overrides the dot color (defaults to `bg-brand`); `textClass` overrides the label color (defaults to `text-black` — pass `text-white` for a label on a dark/media background). |
| LabelPill | `ui/LabelPill.astro` | `class` + slot | Outlined (black border) rounded-full uppercase 14px semibold pill. The card category label ("ALWAYS ON", "HUMANS IN THE LOOP"). Borderless variant is Eyebrow. |
| FeatureCard | `ui/FeatureCard.astro` | `label`, `description`, `class` | Frosted white capability card (`rounded-[24px]`, tall on desktop) with a LabelPill at top and a centered description at bottom. Used in the Care grid. |
| CircleButton | `ui/CircleButton.astro` | `href`, `label`, `class` | 134px circular dotted button (round-capped dashes) with a downward chevron. Inlined SVG from the design. On hover the dotted ring morphs into a solid one. |
| ArrowCircle | `ui/ArrowCircle.astro` | `variant` (`dotted`\|`filled`), `rotate`, `href`, `label`, `detach`, `class` | Small 40px circular arrow button: a dotted ring (round-capped dashes, `0.01 8`) enclosing an inlined right-arrow glyph (recolors via `currentColor`). `dotted` = transparent + black ring + dark arrow (all stat cards), `filled` = purple fill + white arrow, no ring (CTA). `rotate` spins the glyph. `detach` slides the circle 4px away from an adjoining CTA pill on hover (wrap the pill+circle pair in `group`). Renders `<a>` if `href`, else `<button>`. Used across the Numbers cards + CTA, and the navbar CTA. |
| SolutionCard | `ui/SolutionCard.astro` | `theme` (`lime`\|`purple`), `title`, `headline`, `points`, `glow`, `badge?`, `logo?`\|`logoText?`, `logoAlt?`, `stat`, `statLabel`, `cta`, `class` | Use-case capability card. Logo + title row (optional MDR `badge`), Lora headline, proof-point list, white media panel, over a themed glow; plus a "case" bar with a partner `logo` (image) **or** `logoText`, a `stat` + highlight bar, and a Learn More CTA (pill + circle arrow). `lime` = Triage/Clinical; `purple` = Care Orchestration. Content-free — fed from `data/*.json`. |
| StoryCard | `ui/StoryCard.astro` | `type` (`story`\|`quote`\|`logo`\|`highlighted`), `tone` (`white`\|`lavender`\|`outline`), `logo`, `logoAlt`, `title`, `stat`/`stats`, `quote`, `author`, `role`, `cta`/`ctas`, `photo`, `photoAlt`, `class` | Customer-story tile for the "Our Customers" grid. `story` = logo + Lora title + a stat + CTA(s); `quote` = logo + pull-quote + author/role; `logo` = a single centered partner logo; `highlighted` = full-width hero (lavender bloom, big title, two stats, photo panel with overlaid CTA). Pass `photo` to a story/quote for the composite text+photo layout; `tone` sets the surface. Solid CTAs reuse the filled `ArrowCircle`; an outline CTA renders a black-bordered pill. Content-free — fed from `data/customers.json`. |
| TeamCard | `ui/TeamCard.astro` | `name`, `role`, `photo`, `linkedin?`, `class` | Team-member card (About "meet the team"): a `rounded-card` portrait (416×480, object-cover) stacked flush above a white `rounded-card` panel with the name (Figtree semibold 24) + role (`ink-soft` 18) and a decorative `ArrowCircle` (`glyph="linkedin"`) on the right — same dotted ring + hover-to-solid morph as every site circle button. Fully clickable — one stretched overlay `<a>` to the member's LinkedIn (opens in a new tab), the ring is decorative, and card hover morphs its dots into a solid ring and gently zooms the photo. Content-free — fed from `data/about.json` (`team.members`). |
| MediaFrame | `ui/MediaFrame.astro` | `src`, `alt`, `aspect`, `poster`, `controls`, `fill`, `progress`, `class` | Large rounded media container (`rounded-card`) holding an image **or** an autoplaying muted/looping video at a fixed aspect ratio (default 1920/1080). Video sources (`.webm/.mp4/.mov/.ogg`) use `poster` as the first frame; with `controls` (default on) a white play/pause button and a lime-accent mute button overlay the bottom corners. `fill` stretches to the parent box (no radius). `progress` adds a 4px reproduction bar pinned to the bottom edge — faint track + `purple-200` playback fill, click-to-seek. Used for the product demo and the About manifesto hero. |
| ProductVideo | `ui/ProductVideo.astro` | `src`, `poster`, `alt`, `class` | Scroll-driven fullscreen product-video stage wrapping `MediaFrame` (`fill` + `progress`). With JS + motion the frame fills the viewport (`100svh`) and a vanilla rAF scrubber scales it `0.82 → 1 → 0.82` and clips corners `40px → 0` as it passes dead-centre (single script for every `[data-video-scroll]`); no-JS / reduced-motion falls back to the contained rounded 16:9 frame (`max-w-1408`). Shared by the home `Intro` and `SolutionsVideo`. |

## Layout — `src/components/layout/`

| Component | Path | Props | Description |
| --------- | ---- | ----- | ----------- |
| Logo | `layout/Logo.astro` | `href`, `class` | "Sword Intelligence" wordmark, rendered from `public/logotype.svg` (168×21). Accessible label reads `data/site.json`. |
| Navbar | `layout/Navbar.astro` | — | Transparent top bar: logo, centered glass nav pills, and a purple "Book a Meeting" CTA (solid `Button` pill + `filled` `ArrowCircle`, seam-overlapped `-ml-px`). Reads `data/site.json`. |
| LogoStrip | `layout/LogoStrip.astro` | `logos` (`{src, alt}[]`), `class` | Responsive row of partner/client logos, each cell topped by a divider line. Content passed in (home page feeds `data/intro.json`). |

## Sections — `src/components/sections/`

| Component | Path | Props | Description |
| --------- | ---- | ----- | ----------- |
| SwirlBackground | `sections/SwirlBackground.astro` | — | Animated WebGL swirl background (`lib/swirl.ts`, Three.js) recolored to the brand purple palette. One continuous canvas spanning the hero plus a ~45%-of-hero bleed into the section below, with an eased mask that fades it out smoothly across the boundary; static `hero/mesh-gradient.png` is the no-JS / reduced-motion fallback. Place as the first child of a `relative isolate` wrapper around Hero + the next section (see `pages/index.astro`); sizes itself to the first `<section>` in that wrapper (the hero). |
| Hero | `sections/Hero.astro` | — | "Home • Desktop" landing screen: transparent over `SwirlBackground`, navbar, eyebrow, Lora headline, divider cross, paragraph, circle button. Reads `data/hero.json`. |
| Intro | `sections/Intro.astro` | — | "AI Care Managers" section below the hero: transparent over the `SwirlBackground` bleed (which supplies the top wash), centered mixed-weight statement, partner LogoStrip, and the shared `ProductVideo` scroll-driven demo film. Reads `data/intro.json`. |
| Care | `sections/Care.astro` | — | "AI Care Managers" capabilities section: mesh-gradient wash, serif display headline with purple accent, supporting line, 3×2 grid of FeatureCards, and a full-width "Humans in the loop" banner with a dotted-globe graphic. Reads `data/care.json`. |
| Triage | `sections/Triage.astro` | — | "Agentic workforce" use-case section: serif headline with purple accent, a morphing tab/segmented control, and a lime-themed `SolutionCard` (logo + MDR badge, Lora headline, proof points, 10M stat). The card slides in horizontally on first view. On lg (motion allowed) the card column sticks (centered) while extra scroll is scrubbed into a reversible navbar morph: the single "Triage / Clinical Triage" tab expands + crossfades into the four-pill Care Orchestration navbar (first pill purple-active), then the pin releases. Two stacked layers in one grid cell; vanilla `rAF` + `getBoundingClientRect` scrub (no anim lib), width/opacity only. Below lg / `prefers-reduced-motion`: no pin — collapsed Triage tab (or end-state on reduced motion). Reads `data/triage.json` (incl. `orchestration` nav labels). |
| Platform | `sections/Platform.astro` | — | "Clinical Intelligence" system diagram: centered Lora headline + subhead over a fixed-canvas (1280×781) diagram — a glowing orb (CSS inset-shadow bloom + dashed ring + MDR badge) fed on the left by stat cards (85K, 14M) and Unified Clinical Memory, governed on the right (certifications, deterministic logic, protocols), with Patients in at top and Full audits out the bottom, plus full-bleed connector curves. The diagram scales to fit container width via a ResizeObserver (never upscaled). Reads `data/platform.json`. |
| Trust | `sections/Trust.astro` | — | "Trusted by the most regulated industry in the world." section: a rounded lavender panel with a soft blurred purple bloom, a centered Lora headline, a wrapping grid of 14 certification/compliance badges (ISO, MDR, NHS, FDA, HIPAA, GDPR, Cyber Essentials… — transparent pills with an inner purple glow ring, logos composed from native-sized SVG layers), and three bottom points (Proven in practice / Clinically validated / Built for privacy) with `purple-150` icon discs and left hairline rules. Blocks fade-up on reveal. Reads `data/trust.json`. |
| Operations | `sections/Operations.astro` | — | Closing statement banner ("Unbreakable operations. Undeniable outcomes."): a heavily-blurred purple→lime→lavender bloom (`/operations/bloom.svg`) centered behind an `Eyebrow` (with a `purple-500` dot) and a large two-line Lora headline. Content fades up on reveal. Reads `data/operations.json`. |
| Numbers | `sections/Numbers.astro` | — | Proof-point bento grid: stat cards (4x intake with lime radar rings, 5.2x ROI with a CSS dot-grid bar chart, 34% readmissions on an NHS card, 686 workdays on lime), a customer portrait, and a ministerial testimonial (`purple-tint` card), closed by a "Book a Meeting" CTA. Cards stack on mobile; at md+ they snap into the design's exact bento via absolute positioning on a 1280×877 canvas (per-card `--l/--t/--w/--h`). Uses `ArrowCircle`. Reads `data/numbers.json`. |
| Integrations | `sections/Integrations.astro` | — | "The clinical intelligence layer between your patients and your tech stack." A centered Lora headline over a fixed-canvas (1440×920) isometric diagram: three patient avatars drop dotted lines into a perspective grid plane (`/clinical/grid.svg`, flow ribbons baked in), where the AI layer fans out to five channels — SMS, Phone calls, Chat apps, Calendar invite, E-mail — each a `bg-brand` pill + frosted `bg-ink-wash` icon chip. A "Ready to integrate" card anchors the left, a soft purple→blue→lavender bloom sits behind. Diagram scales to fit container width via a ResizeObserver (never upscaled), same pattern as Platform. Reads `data/integrations.json`. |
| Dashboards | `sections/Dashboards.astro` | — | "Complex operations? Complete control." A two-column section: left is a centered sans (`font-medium`) heading + supporting line (`dashboards` bold) over a vertical stack of three frosted-lavender (`bg-purple-tint`, `rounded-card`) capability cards — each a 40px icon disc (`/dashboards/icon-{monitor,watchtower,agents}.svg`, line glyphs sliced from the export) above a short statement with a bold phrase; right is the large isometric dashboard illustration (`/dashboards/illustration.svg`, cropped from the design's 1440×900 export to its content bbox, soft blur shadow preserved). Stacks to one centered column below `lg`; blocks fade up on reveal (heading uses the `title` blur-resolve). Reads `data/dashboards.json`. Sits between `Integrations` and `Scaling` on the home page. |
| Cta | `sections/Cta.astro` | — | Closing call-to-action band (Figma "CTA", 1440×519): a static `purple-200` dotted mesh drawn on a `<canvas>`, reproducing the design exactly — dot radius grows with distance from a focal point at (40.3%, 11.6%), so dots vanish upper-center-left (clearing the headline) and bloom solid at the edges. The pointer **adds a blur on top**: while the cursor moves over the band a soft blurred brand-purple bloom (halo + brightened dots) is generated under it, easing in on enter and out on leave. Over it sits a centered Lora headline with a purple accent ("Good things come to those who *don't wait*.") and a "Get started" pill + `filled` `ArrowCircle`. Mesh colors read from tokens via `getComputedStyle`; honors `prefers-reduced-motion` (static field only, no pointer blur). Reads `data/cta.json`. |
| Footer | `sections/Footer.astro` | — | Site footer: a lime (`bg-accent`) panel in black ink. A Lora tagline ("Built with care, by people who truly understand it.") sits top-left beside a small column of outbound social links (uppercase label + inline arrow-up-right glyph that nudges on hover); a hairline-ruled (`border-y-[1.5px]`) legal bar spreads copyright · Cookie Settings · Privacy Policy across the content width; and the full-bleed "sword Intelligence" wordmark (`public/footer/logotype-wordmark.svg` — a composed vector lockup, Figtree "sword" + Lora "Intelligence") anchors the bottom. Reads `data/footer.json`. |
| Customers | `sections/Customers.astro` | — | The "Our Customers" page body (Figma "CustomerStories"): a centered Lora title over an editorial masonry of `StoryCard`s. Rows are CSS grids whose column templates are fixed by the design (full / half / thirds / 416+848 / 848+416) and keyed off each row's `layout`; on mobile every card stacks to one column. A lavender→surface page wash sits behind, closed by a "load more" CTA (purple pill + plus circle). Reads `data/customers.json`. Lives on `src/pages/customers.astro` with the same footer-reveal shell as the home page. |
| CustomerStory | `sections/CustomerStory.astro` | `story` | A single customer-story page body (Figma "CustomerStories>NHS"). Three sections fed from one entry of `data/customer-stories.json`: a full-bleed photo **Hero** (transparent→lavender→surface blur wash) with the customer logo, an `Eyebrow` case-study label, a big Lora title and a 3-up row of left-hairline stat columns; an **Overview** band (left "ID" fact sheet on hairline rules — customer type / patients served / location / use-case chips — beside a Lora heading + body paragraphs, which allow inline `<a>`/`<strong>` via `set:html`); and a centered **Testimonial** (Eyebrow, Patient/Customer segmented control, big pull-quote, avatar + author). Content-free. Lives on `src/pages/customers/[slug].astro` (one page per data entry) with the home page's footer-reveal shell; the "Read more" CTAs on the Our Customers grid link here. |
| AboutHero | `sections/AboutHero.astro` | — | Full-viewport manifesto hero for the About page with a scroll-driven transition (Figma `8616:10`). At the top the manifesto film fills the viewport and plays (`MediaFrame` in `fill` mode, navbar overlaid, play/pause + mute controls, `progress` bar). The section is taller than the viewport with a `sticky` stage; a vanilla rAF scrubber pins it and runs a timeline as you scroll: the controls + bar fade out, the film pauses + dims under a `bg-ink` scrim, then the mission `Eyebrow` (lime dot, white label) + Lora statement resolve out of a blur (the house "title" reveal) bottom-left. No-JS / reduced-motion falls back to the Figma end state (blurred film + visible statement). Reads `data/about.json`. Lives on `src/pages/about.astro`. |
| AboutStats | `sections/AboutStats.astro` | — | "The story so far" proof section on the About page (Figma `8616:408`, "Section-02"). A header row — `Eyebrow` (`purple-500` dot) + Lora title left, a two-paragraph body (second semibold) right — over a 3×2 grid of stat cards (10M+ sessions / 100M+ lives / 58 papers+patents / 50 patents / 5 continents / $4B valuation). Each card is a `rounded-card` outlined panel (`border-black/24`) with a `purple-150` icon disc top and, pinned bottom, a big Lora number (optional frosted `bg-ink-wash-strong` "+" chip) over a `text-sm` uppercase caption — the Scaling number/chip vocabulary in card form. Closed by a `purple-tint` "Trusted by" panel (`LabelPill` + wrapping logo row: Elevance, Independence, Highmark, CareFirst, Fresenius, Generali — black SVGs in `public/about/logos`, Fresenius assembled from its Figma vector fragments). Cards stack on mobile, 2-up at sm, 3-up at lg; header + cards fade up on reveal. Reads `data/about.json` (`storySoFar`). Lives on `src/pages/about.astro`. |
| AboutOperations | `sections/AboutOperations.astro` | — | Scroll-pinned, two-state beat on the About page. State A (Figma `8618:517`, "Section-04") is a centered Lora statement ("We first built AI…") over a Figtree body; as you scroll it fades + blurs out and State B (Figma `8618:522`, "Section-05") resolves out of a blur — the statement becomes "So, Sword Intelligence was born.", the body swaps, and three proof cards stagger up: two big-number stat cards (85K+ physician hours, 14M+ care interactions) and a frosted `Medical certifications` panel (`bg-ink-wash-strong`, badges in one nowrap row). Cards use the native Figma row geometry (`grid-cols-[250.28fr_250.28fr_407.44fr]`, 940px, 16px gaps) at `lg`, stacking 1-up→2-up below; no drop shadow — the two white stat cards get a `black/12` hairline border, the cert panel a soft grey fill. Badges are standalone SVGs sliced from the design export — `/about/certs/{iso,mdr,mhra}.svg` (ISO 13485/27001, MDR Class IIa, MHRA Class I & IIa). Mechanics mirror `AboutHero`: a `sticky` stage in a taller section driven by a vanilla rAF scrubber; no-JS / reduced-motion renders both states as normal stacked flow. Content-free — reads `data/about.json` (`operations.intro` / `operations.born`). Lives on `src/pages/about.astro` after `AboutStats`. |
| AboutStory | `sections/AboutStory.astro` | — | The manifesto text block below the About hero (Figma `8616:406`): two large Figtree paragraphs in a narrow left-aligned column, one semibold phrase ("by making healthcare more intelligent") in the second. Effect (matches the live site): a scroll-driven letter-by-letter reveal — each character starts a muted gray (`--color-ink-muted` `#BFC0CC`) and "lights up" to solid black as a reading head tracks scroll progress through the block (vanilla rAF scrubber, soft `FADE`-letter trailing band, `easeOutCubic`). Words are wrapped `nowrap` so letters never split across lines. No-JS / reduced-motion renders every letter solid black (the Figma end state). Reads `data/about.json` (`story`). Lives on `src/pages/about.astro`. |
| AboutCta | `sections/AboutCta.astro` | — | Closing call-to-action on the About page (Figma "Platform", `8628:8`). Unlike the home `Cta` mesh band, this is a centered white `rounded-card` (max-w 1040) on a `purple-tint` section: a two-line Lora headline ("Built inside Sword. / Proven at scale. Ready for your teams.") over a brand-purple pill ("Let's make you AI-first, too") seamed (`-ml-px`) to a `filled` `ArrowCircle`. Pill+arrow ride the shared `[data-reveal]` entrance. Reads `data/about.json` (`cta`). Lives on `src/pages/about.astro` after `AboutOperations`. |
| AboutTeam | `sections/AboutTeam.astro` | — | "meet the team" section on the About page (Figma "Section-06", `8628:21`). A centered `Eyebrow` (brand dot, black label) + large Lora title over a 3×2 grid of `TeamCard`s (Virgílio Bento, Rik Renard, Mark Ratnarajah, João Pereira, Filipa Alcobia Ferreira, Selina Sachar — portraits in `public/about/team`, exported from the design and downscaled to JPEG). Cards stack 1-up on mobile, 2-up at sm, 3-up at lg; header + cards fade up on reveal (staggered per column). Content-free — reads `data/about.json` (`team`). Lives on `src/pages/about.astro` after `AboutCta`. |
| SolutionsHero | `sections/SolutionsHero.astro` | — | "Solution • Desktop" landing hero (Figma `8634:517`): a `bg-purple-tint` screen with the navbar overlaid, an `Eyebrow` ("Surgery prehab and rehab") above a full-width `border-gray-200` hairline divider, then a large Lora headline ("Prepare every patient, support every recovery.") + Figtree paragraph on the left and a white `rounded-[32px]` media/illustration placeholder card on the right (per the design annotation — meant to receive motion/illustrations). Left text column uses `justify-between` to seat the paragraph at the bottom like the design; stacks above the card on mobile, side-by-side at `lg`. Blocks ride the shared `[data-reveal]` entrance. Reads `data/solutions.json`. Lives on `src/pages/solutions.astro`. |
| SolutionsLogos | `sections/SolutionsLogos.astro` | — | Partner-logo strip on the Solutions page (Figma `8634:646`, "Logos"): a centered uppercase `text-sm` title ("Join the Healthcare Organizations Scaling with Intelligence", max-w 962) over the shared `LogoStrip` of five healthcare-org logos (Hellenic Ministry of Health, NHS, North East London, Cheshire & Merseyside, VivaMás — the same `/logos/*` assets the home `Intro` uses), passed `borderClass="border-black/24"` for the design's faint top rules. 80px (`gap-20`) between title and row; `pt-20 pb-[200px]`. Title + strip fade up on reveal. Reads `data/solutions.json` (`logos`). Lives on `src/pages/solutions.astro` after `SolutionsHero`. |
| SolutionsVideo | `sections/SolutionsVideo.astro` | — | The home page's scroll-driven product demo film on the Solutions page: a thin `bg-surface` wrapper around the shared `ProductVideo` stage, reusing the same `/intro/product.webm` source. Reads `data/solutions.json` (`media`). Lives on `src/pages/solutions.astro` after `SolutionsHandoff`. |
| SolutionsHandoff | `sections/SolutionsHandoff.astro` | — | Pinned **Problem → Solution** beat on the Solutions page (Figma `8634:669` + `8615:4005`). Two full panels crossfade as one while the stage is `sticky` (mechanics mirror `AboutOperations`: section taller than viewport, rAF scrubber maps scroll progress → Problem fades + blurs out, Solution resolves out of blur, then its cards stagger up; gated on motion + `js` + `min-width:768px`, else normal stacked flow). **State A "Problem"**: `Eyebrow` + Lora heading + subheading over a lavender-grey panel (`bg-ink-wash` + `/solutions/problem-bloom.svg`) of four `bg-gray-200/50` stat cards (`text-gray-400`); each card's dotted `ArrowCircle` "i" toggles `[data-open]` to reveal a flush white source-link panel (max-height grow on an `items-stretch` row). **State B "Solution"**: purple-accent Lora heading + subheading over a `bg-purple-tint` panel (`/solutions/solution-bloom.svg`) of four white→purple outcome cards (`bg-[image:var(--gradient-solution-card)]`, black Lora value, dotted arrow `ArrowCircle`). Reads `data/solutions.json` (`problem`, `solution`). Lives on `src/pages/solutions.astro` after `SolutionsLogos`. |
| SolutionsHowItWorks | `sections/SolutionsHowItWorks.astro` | — | "HOW IT WORKS / Enroll. Coach. Prepare. Recover." stepper on the Solutions page (Figma `8679:2`). A centered header (`Eyebrow` with `purple-500` dot + Lora headline + a `border-black/24`-divided row of platform capabilities) over an interactive stepper: a lateral icon navbar (the design's `bg-ink-wash` pill of five `gray-200`-outlined 40px chips) selects one of five steps, each a card with a left text column (uppercase eyebrow / Lora heading / body) and a white media placeholder (`h-616 w-522` at lg). Icons are line glyphs sliced from the export and inlined with `currentColor` so the active chip lights up in the brand purple (`aria-selected:text-brand`); per the design annotation the icon set is meant to vary per solution. All five panels share one CSS grid cell (`grid-area:1/1`) so the area sizes to the tallest step and panels crossfade in place via the `[hidden]` attribute — no layout jump; tablist a11y (roving arrow keys, `aria-selected`/`aria-controls`). Below `lg` the navbar is a horizontal row above the card and panels stack text over media. Header + stepper fade up on reveal. Reads `data/solutions.json` (`howItWorks`). Lives on `src/pages/solutions.astro` after `SolutionsVideo`. |
| Scaling | `sections/Scaling.astro` | — | "Scaling care with AI: been there, done that." Editorial proof section: an `Eyebrow` (`purple-500` dot) over a two-column row — Lora display headline with a purple "done that" accent on the left, supporting paragraph on the right — above a four-up row of company stats (10M+ sessions, 100M+ lives, 108+ papers/patents, $4B+ valuation). Each stat is a left-hairline column with a lime (`bg-accent`) icon disc, a big Lora number + frosted (`bg-ink-wash-strong`) "+" chip, and a caption. A white→lime mesh gradient blooms from the right. Columns stack on mobile, 2-up at sm, 4-up at lg. Blocks fade up on reveal. Reads `data/scaling.json`. |
